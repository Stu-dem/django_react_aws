name: Build and deploy frontend
env: 
  AWS_STORAGE_BUCKET_NAME: "django-react-frontend"
  S3_STATICFILES_NAME: "static"
  AWS_REGION_NAME: "us-east-2"

on:
  push:
    branches: [ master ]
    
jobs:
  copy-django-static:
    name: Copy django staticfiles
    runs-on: ubuntu-latest
    defaults:
      run:
          working-directory: ./
    steps: 
      - name: Git clone on our repo
        uses: actions/checkout@v2

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION_NAME }}

      - name: Copying file to S3
        run: aws s3 cp ${{ env.DEPLOY_PACKAGE_NAME }} s3://${{ env.AWS_STORAGE_BUCKET_NAME }}/

      - name: Print nice message on success finish
        run: echo "CI part finished successfuly"
    
  build-test-deploy:
    name: Tests
    runs-on: ubuntu-latest
    defaults: 
      run:
        working-directory: ./frontend
    steps:
      - uses: actions/checkout@v2

      - name: Injecting environment variables
        run: echo "REACT_APP_API_URL=${{ secrets.API_URL }}" >> .env

      - name: Installing dependencies
        run: yarn install

      - name: Running tests
        run: yarn test

      - name: Building project
        run: yarn build

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION_NAME }}

      - name: Deploy to S3 bucket
        run: aws s3 sync ./build/ s3://${{ secrets.AWS_STORAGE_BUCKET_NAME }} --delete